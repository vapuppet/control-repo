{
  "name": "codedeploy",
  "description": "provisions vm in terraform and adds agent to server",
  "private": true,
  "input_method": "environment"
  "parameters": {
    "environment": {
      "description": "Specify environment to deploy to",
      "type": string,
      "sensitive": false
    }
  }
}
